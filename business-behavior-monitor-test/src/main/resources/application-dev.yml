server:
  port: 8091

business:
  behavior:
    monitor:
      config:
        # 启用状态
        enable: true
        # Redis 配置
        host: 127.0.0.1
        port: 6379

logging:
  level:
    root: info
  config: classpath:logback-spring.xml